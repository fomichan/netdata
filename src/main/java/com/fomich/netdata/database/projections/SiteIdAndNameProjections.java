package com.fomich.netdata.database.projections;

public interface SiteIdAndNameProjections {   // Не использую. Чтобы читать только ID и Name для отображения
    Integer getId();
    String getName();
}
