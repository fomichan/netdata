package com.fomich.netdata.dto;

public record SiteFilter(
                                String name,
                                String region,
                                String city,
                                String address
                          ) {
}
